version: '3.8'
services:
  staking_client:
    build:
      context: ..
      dockerfile: staking_client/Dockerfile
    env_file:
      - .env
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
